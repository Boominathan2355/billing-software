services:
  - type: web
    name: billing-server
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build-render
    startCommand: npm start -w server
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        fromConfig: true
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 5000
